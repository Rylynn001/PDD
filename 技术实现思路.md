# PDD 交易数据工具 — 技术实现思路

## 核心问题

拼多多商家后台的接口有反爬保护：每次请求都需要一个动态生成的 `anti-content` 字段，这个字段是浏览器里的 JS 代码实时算出来的，没法直接伪造。

---

## 解决思路

**让真实的 Chrome 浏览器帮我们发请求，然后把它的请求头偷出来。**

1. 用 Playwright 控制一个 Chrome 窗口，打开拼多多商家后台。
2. 用户在这个窗口里正常登录一次（登录状态会存在本地，下次不用再登录）。
3. 需要拉数据时，让 Chrome 在拼多多页面的上下文里发一次 fetch 请求。
4. 在请求离开浏览器之前把它拦截下来，读出 `anti-content` 和 `cookie`。
5. 拿到这两个参数后，Python 直接用 `requests` 调拼多多 API 拉数据，不再绕弯子。

---

## 整体架构

```
前端 (Vue3)  →  FastAPI 后端  →  拼多多 API
                    ↕
              Chrome (Playwright)
```

- **前端**：Vue3 + Element Plus，提供日期选择、一键捕获认证、查询、导出 Excel 的界面。
- **后端**：FastAPI，负责启动 Chrome、捕获认证参数、转发 API 请求、过滤数据、生成 Excel。
- **Chrome 控制**：用 `sync_playwright` 在独立线程里运行，避免和 FastAPI 的异步事件循环冲突。

---

## 数据流

```
点击"自动捕获认证"
  → 后端让 Chrome 在拼多多页面发一次假请求
  → 拦截请求头，提取 anti-content + cookie
  → 返回给前端自动填充

点击"查询数据"
  → 前端把 anti、cookie、日期范围发给后端
  → 后端用这两个参数直接请求拼多多 API
  → API 返回的数据比请求日期多 6-7 天，后端按日期严格过滤
  → 返回筛选后的 11 个核心指标

点击"导出 Excel"
  → 后端用 pandas 把数据写成 .xlsx 文件流
  → 浏览器直接下载
```

---

## 关键细节

| 问题 | 处理方式 |
|------|----------|
| anti-content 无法伪造 | 在 Chrome 页面上下文内触发 fetch，浏览器自动注入 |
| 登录状态保持 | Playwright 持久化 user_data 目录，登录一次永久有效 |
| API 多返回 6-7 天 | 后端按 `stateDate` 字段做字符串大小比较，严格裁剪 |
| Windows asyncio 兼容 | sync_playwright 跑在独立线程池，不依赖 uvicorn 的事件循环 |
| 百分比字段 | `payUvRto`、`rpayUsrRtoDth` 原始值是小数，×100 转成百分比显示 |
